import { CategoryType } from "./categoryType";

export class Category {
    categoryId!: number;
    categoryName!: string;
    categorytype!: CategoryType;
    userId!: number;
    username!: string;
    email!: string;
}