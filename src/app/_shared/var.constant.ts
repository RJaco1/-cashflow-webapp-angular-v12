import { environment } from "@envs/environment";

export const HOST = environment.HOST;
export const TOKEN_AUTH_USERNAME = environment.TOKEN_AUTH_USERNAME;
export const TOKEN_AUTH_PASSWORD = environment.TOKEN_AUTH_PASSWORD;
export const TOKEN_NAME = environment.TOKEN_NAME;
export const RETRIES = 3;